{
  "name": "ralph-works",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight RalphWorks workflow coordinator for the Pi agent harness.",
  "scripts": {
    "test": "node --test"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
